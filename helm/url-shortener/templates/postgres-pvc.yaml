apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "url-shortener.fullname" . }}-postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.postgres.storage.size }}